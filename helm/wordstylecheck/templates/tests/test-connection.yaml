apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wordstylecheck.fullname" . }}-test-connection"
  labels:
    {{- include "wordstylecheck.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wordstylecheck.fullname" . }}-foreman:{{ .Values.service.port }}']
  restartPolicy: Never
